import { describe, it } from 'vitest';

describe('MonthSelector', () => {
  it('renders previous button.', () => {});
  it('renders current button.', () => {});
  it('when current button is clicked, renders select month drawer.', () => {});
  it('renders next button.', () => {});
});
