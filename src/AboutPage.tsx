import { NavLink } from 'react-router';

export default function AboutPage() {
  return (
    <div>
      AboutPage
      <div>
        <NavLink to="/">to HomePage</NavLink>
      </div>
    </div>
  );
}
